const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const p = await prisma.player.findFirst({ where: { OR: [ { avatarImage: { startsWith: 'data:image' } }, { coverBanner: { startsWith: 'data:image' } } ] } }); if (p) { console.log('Avatar length:', p.avatarImage ? p.avatarImage.length : 0); console.log('Cover length:', p.coverBanner ? p.coverBanner.length : 0); console.log('Avatar ends with:', p.avatarImage ? p.avatarImage.slice(-20) : null); console.log('Cover ends with:', p.coverBanner ? p.coverBanner.slice(-20) : null); } } main().finally(() => prisma.$disconnect());
