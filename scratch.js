const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const players = await prisma.player.findMany({ select: { name: true, coverBanner: true }}); console.log('Players with cover:', players.filter(p => p.coverBanner && p.coverBanner.length > 5).map(p => ({name: p.name, coverType: p.coverBanner.substring(0, 30)}))); } main().finally(() => prisma.$disconnect());
